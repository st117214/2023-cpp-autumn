#ifndef parcingfunctionsh
#define parcingfunctionsh

#include <string>

std::string removeSpaces(const std::string& str);
void parseTerm(const std::string& term, double& a, double& b, double& c, double& d, double& e, double& f);

#endif
